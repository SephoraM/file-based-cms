<!DOCTYPE html>
<html>
  <head>
    <title>CMS app</title>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/css/application.css">
  </head>
  <body>
    <% if session[:message] %>
      <p class="message"><%= session.delete(:message) %></p>
    <% end %>
    <%== yield %>
  </body>
</html>
